export async function fetchFinancials(
  clientKey: string,
  monthLabel: string
): Promise<any> {
  return {};
}

export async function fetchReferencingCommission(clientKey: string): Promise<any> {
  return {};
}
