diff --git a/lib/supabaseLogistics.ts b/lib/supabaseLogistics.ts
index 3447fdf..f8c18f3 100644
--- a/lib/supabaseLogistics.ts
+++ b/lib/supabaseLogistics.ts
@@ -16,14 +16,18 @@ import { createClient, SupabaseClient } from "@supabase/supabase-js";
 import type { ClientConfig, CountryStats, DelayBuckets, GeodisResult, GlsResult } from "./types";
 
 function getSupabase(): SupabaseClient {
-  const url = process.env.NEXT_PUBLIC_SUPABASE_URL || "";
-  const key = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || "";
+  const url = process.env.NEXT_PUBLIC_SUPABASE_URL || process.env.SUPABASE_URL || "";
+  // Les tables logistiques sont sous RLS (lecture réservée au rôle authenticated) :
+  // côté serveur on utilise la clé service. La clé anon reste un repli mais ne
+  // verra aucune ligne tant qu'aucune policy anon n'existe.
+  const key =
+    process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY || "";
   if (!url || !key) {
     throw new Error(
-      "Variables d'environnement Supabase manquantes (NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY)"
+      "Variables d'environnement Supabase manquantes (NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY)"
     );
   }
-  return createClient(url, key);
+  return createClient(url, key, { auth: { persistSession: false } });
 }
 
 // ---------------------------------------------------------------------------
